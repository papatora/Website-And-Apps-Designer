# Agent instructions

Read `CLAUDE.md` for the repo map and commands, and `docs/anti-slop.md` for
the design rules. When building UI with this kit, follow
`.claude/skills/anti-slop-design/SKILL.md` — it applies to any coding agent,
not only Claude Code. Run `npm run check` before finishing.
